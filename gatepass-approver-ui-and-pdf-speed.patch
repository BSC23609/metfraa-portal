diff --git a/app/routes/gatepass_public.py b/app/routes/gatepass_public.py
index 20bc6ea..136310a 100644
--- a/app/routes/gatepass_public.py
+++ b/app/routes/gatepass_public.py
@@ -144,8 +144,12 @@ def download_pass(token: str, db: Session = Depends(get_db)):
                      "Please try again, or open it from the portal.", 500)
     safe = (o.ref_no or "pass").replace("/", "-")
     name = f"{(o.requester.name if o.requester else 'pass')} {safe}.pdf"
+    # An approved pass never changes, so let the phone keep it. Re-opening the
+    # WhatsApp link then costs nothing instead of a fresh round trip on mobile
+    # data — which is most of what "takes forever" actually is.
     return Response(content=pdf, media_type="application/pdf",
-                    headers={"Content-Disposition": f'inline; filename="{name}"'})
+                    headers={"Content-Disposition": f'inline; filename="{name}"',
+                             "Cache-Control": "private, max-age=604800, immutable"})
 
 
 @router.get("/ogb/{token}", response_class=HTMLResponse)
diff --git a/app/services/outpass_pdf.py b/app/services/outpass_pdf.py
index 6abc783..14e6861 100644
--- a/app/services/outpass_pdf.py
+++ b/app/services/outpass_pdf.py
@@ -27,12 +27,49 @@ METFRAA_LOGO = _ASSETS / "metfraa-logo.png"      # 500x138
 GROUP_LOGO = _ASSETS / "group-logo.png"          # 375x133
 
 
+# Logos are ~45 KB of a 47 KB pass when embedded at source resolution, for a
+# 9 mm-tall print. Downscale once and cache: the file drops to a few KB, which
+# is what makes it open quickly on mobile data.
+_LOGO_CACHE: dict = {}
+_LOGO_PX_H = 90          # ~250 dpi at 9 mm — sharp in print, tiny on the wire
+
+
+def _logo(path: pathlib.Path):
+    key = str(path)
+    if key in _LOGO_CACHE:
+        return _LOGO_CACHE[key]
+    from io import BytesIO
+
+    from reportlab.lib.utils import ImageReader
+    try:
+        from PIL import Image
+    except ImportError:
+        # Pillow ships with reportlab, but if it ever isn't there a bigger
+        # pass is far better than no pass at the gate.
+        reader = ImageReader(str(path))
+        _LOGO_CACHE[key] = reader
+        return reader
+    im = Image.open(path).convert("RGB")
+    if im.height > _LOGO_PX_H:
+        im = im.resize((max(1, round(im.width * _LOGO_PX_H / im.height)), _LOGO_PX_H),
+                       Image.LANCZOS)
+    # JPEG, not PNG: reportlab passes a JPEG straight through as DCTDecode,
+    # whereas a PNG is re-encoded as raw pixels and balloons the file. The
+    # logos are photographic-ish on white, so JPEG artefacts don't show at
+    # 9 mm. This is the difference between a 47 KB and a ~20 KB pass.
+    buf = BytesIO()
+    im.save(buf, format="JPEG", quality=88, optimize=True)
+    buf.seek(0)
+    reader = ImageReader(buf)
+    _LOGO_CACHE[key] = reader
+    return reader
+
+
 def _draw_logo(c, path: pathlib.Path, x, y, max_h, right_align_at=None):
     """Draw a logo at its natural aspect ratio. Missing file is not fatal —
     a pass without a logo still works at the gate; a crash doesn't."""
     try:
-        from reportlab.lib.utils import ImageReader
-        img = ImageReader(str(path))
+        img = _logo(path)
         iw, ih = img.getSize()
         h = max_h
         w = h * iw / ih
diff --git a/app/templates/gatepass.html b/app/templates/gatepass.html
index 70b379e..e4c09d1 100644
--- a/app/templates/gatepass.html
+++ b/app/templates/gatepass.html
@@ -306,6 +306,21 @@
         The approver-overrides table hasn't been created yet, so assignments can't
         be saved. Run the database migrations, then reload this page.
       </div>
+      <div x-show="dirtyCount > 0"
+           class="flex items-center justify-between gap-3 bg-metfraa-blue/5 ring-1 ring-metfraa-blue/20 rounded p-3 mb-3">
+        <div class="text-sm text-metfraa-ink">
+          <span class="font-bold" x-text="dirtyCount"></span>
+          unsaved change<span x-show="dirtyCount !== 1">s</span>
+        </div>
+        <div class="flex gap-2">
+          <button @click="loadEmps()"
+            class="px-3 py-1.5 rounded-lg ring-1 ring-metfraa-gray-300 text-xs font-bold">Discard</button>
+          <button @click="saveAll()" :disabled="savingAll"
+            class="px-4 py-1.5 rounded-lg bg-metfraa-blue text-white text-xs font-bold disabled:opacity-50">
+            <span x-text="savingAll ? 'Saving…' : 'Save all'"></span>
+          </button>
+        </div>
+      </div>
       <div class="text-xs text-metfraa-gray-500 mb-3">
         <span class="font-semibold" x-text="unsetCount"></span> of
         <span x-text="empRows.length"></span> without their own approver
@@ -335,26 +350,31 @@
                   </div>
                 </td>
                 <td class="py-2 pr-2">
-                  <select x-model.number="e.approver_emp_id"
+                  <select @change="setApprover(e, 'approver_emp_id', $event.target.value)"
                           class="text-xs rounded border-metfraa-gray-300 w-full">
-                    <option :value="null">— none —</option>
+                    <option value="" :selected="!e.approver_emp_id">— none —</option>
                     <template x-for="c in choices" :key="c.id">
-                      <option :value="c.id" :disabled="c.id === e.id" x-text="c.name"></option>
+                      <option :value="c.id" :disabled="c.id === e.id"
+                              :selected="c.id === e.approver_emp_id" x-text="c.name"></option>
                     </template>
                   </select>
                 </td>
                 <td class="py-2 pr-2">
-                  <select x-model.number="e.leave_cover_emp_id"
+                  <select @change="setApprover(e, 'leave_cover_emp_id', $event.target.value)"
                           class="text-xs rounded border-metfraa-gray-300 w-full">
-                    <option :value="null">— none —</option>
+                    <option value="" :selected="!e.leave_cover_emp_id">— none —</option>
                     <template x-for="c in choices" :key="c.id">
-                      <option :value="c.id" :disabled="c.id === e.id" x-text="c.name"></option>
+                      <option :value="c.id" :disabled="c.id === e.id"
+                              :selected="c.id === e.leave_cover_emp_id" x-text="c.name"></option>
                     </template>
                   </select>
                 </td>
                 <td class="py-2 text-right whitespace-nowrap">
                   <button @click="saveEmp(e)"
-                    class="px-3 py-1 rounded-lg bg-metfraa-blue text-white text-xs font-bold">Save</button>
+                    :class="e._dirty ? 'bg-metfraa-blue text-white' : 'ring-1 ring-metfraa-gray-300 text-metfraa-gray-500'"
+                    class="px-3 py-1 rounded-lg text-xs font-bold">
+                    <span x-text="e._dirty ? 'Save' : 'Saved'"></span>
+                  </button>
                 </td>
               </tr>
             </template>
@@ -372,7 +392,7 @@
 function gpApp() {
   return {
     tab: 'new', busy: false, msg: '', err: false,
-    mine: [], approvals: [], openPasses: [], depts: [], people: [], empRows: [], choices: [], empQ: '', empSchemaReady: true, wa: { configured: false, hr_phone_set: false, counts: {}, rows: [] },
+    mine: [], approvals: [], openPasses: [], depts: [], people: [], empRows: [], choices: [], empQ: '', empSchemaReady: true, savingAll: false, wa: { configured: false, hr_phone_set: false, counts: {}, rows: [] },
     myApprover: null, pendingCount: {{ pending_for_me }},
     isAdmin: {{ 'true' if is_admin else 'false' }},
     form: { type: 'outpass', req_date: new Date().toISOString().slice(0, 10),
@@ -424,6 +444,45 @@ function gpApp() {
         (e.department || '').toLowerCase().includes(q));
     },
     get unsetCount() { return this.empRows.filter(e => !e.approver_emp_id).length; },
+    get dirtyCount() { return this.empRows.filter(e => e._dirty).length; },
+
+    setApprover(e, field, raw) {
+      e[field] = raw === '' ? null : Number(raw);
+      e._dirty = true;
+    },
+
+    async saveAll() {
+      const pending = this.empRows.filter(e => e._dirty);
+      if (!pending.length) return;
+      this.savingAll = true;
+      const failed = [];
+      try {
+        // Sequential on purpose — a burst of parallel writes against a
+        // serverless function is how you get connection-pool errors.
+        for (const e of pending) {
+          const ok = await this.postApprover(e);
+          if (!ok) failed.push(e.name);
+        }
+      } finally {
+        this.savingAll = false;
+      }
+      if (failed.length) alert('Could not save: ' + failed.join(', '));
+      this.loadEmps();
+    },
+
+    async postApprover(e) {
+      try {
+        const r = await fetch('/gatepass/api/admin/employee-approvers', {
+          method: 'POST', headers: { 'Content-Type': 'application/json' },
+          body: JSON.stringify({ employee_id: e.id,
+            approver_emp_id: e.approver_emp_id || null,
+            leave_cover_emp_id: e.leave_cover_emp_id || null }),
+        });
+        if (!r.ok) return false;
+        e._dirty = false;
+        return true;
+      } catch (err) { return false; }
+    },
 
     async loadEmps() {
       const r = await fetch('/gatepass/api/admin/employee-approvers');
@@ -438,13 +497,7 @@ function gpApp() {
       this.empSchemaReady = d.schema_ready !== false;
     },
     async saveEmp(e) {
-      const r = await fetch('/gatepass/api/admin/employee-approvers', {
-        method: 'POST', headers: { 'Content-Type': 'application/json' },
-        body: JSON.stringify({ employee_id: e.id,
-          approver_emp_id: e.approver_emp_id || null,
-          leave_cover_emp_id: e.leave_cover_emp_id || null }),
-      });
-      if (!r.ok) { const d = await r.json().catch(() => ({})); alert(d.detail || 'Failed'); return; }
+      if (!(await this.postApprover(e))) { alert('Could not save ' + e.name); return; }
       this.loadEmps();
     },
     async loadDepts() {
diff --git a/requirements.txt b/requirements.txt
index 4684e11..0c50298 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -8,6 +8,7 @@ msal>=1.31.1
 httpx>=0.28.1
 openpyxl>=3.1.5
 reportlab>=4.2.5
+pillow>=10.0.0        # pulled in by reportlab, declared because we use it directly
 matplotlib>=3.9.3
 python-dotenv>=1.0.1
 pydantic>=2.10.3
