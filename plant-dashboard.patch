diff --git a/app/routes/plant.py b/app/routes/plant.py
index b4ca273..18a14ec 100644
--- a/app/routes/plant.py
+++ b/app/routes/plant.py
@@ -618,6 +618,88 @@ async def api_settings_save(request: Request,
     db.commit()
     return {"ok": True, "labour_ot_rate_per_half_hour": rate}
 
+# --------------------------------------------------------------- dashboard
+
+@router.get("/api/dashboard")
+def api_dashboard(start: str | None = None, end: str | None = None,
+                  user: Employee = Depends(get_current_user),
+                  db: Session = Depends(get_db)):
+    """Aggregates for the dashboard over a date range:
+      * daily attendance trend (present/half/absent + contractor headcount)
+      * qty & weight produced per day (own team + contractors combined)
+      * qty & weight totals per contractor team (own team shown as 'Own team')
+      * headline totals for the range."""
+    _guard(db, user)
+    e = _parse_date(end)
+    s_ = _parse_date(start)
+    if s_ > e:
+        s_, e = e, s_
+
+    from collections import defaultdict
+    trend = defaultdict(lambda: {"present": 0, "half": 0, "absent": 0,
+                                 "contractor": 0, "qty": 0.0, "weight": 0.0})
+
+    for a in (db.query(PlantLabourAttendance)
+              .filter(PlantLabourAttendance.att_date >= s_,
+                      PlantLabourAttendance.att_date <= e).all()):
+        k = a.att_date.isoformat()
+        if a.status == "P": trend[k]["present"] += 1
+        elif a.status == "H": trend[k]["half"] += 1
+        elif a.status == "A": trend[k]["absent"] += 1
+    for a in (db.query(PlantContractorAttendance)
+              .filter(PlantContractorAttendance.att_date >= s_,
+                      PlantContractorAttendance.att_date <= e).all()):
+        trend[a.att_date.isoformat()]["contractor"] += a.skilled + a.helper
+
+    cmap = {c.id: c.name for c in db.query(PlantContractor).all()}
+    per_team = defaultdict(lambda: {"qty": 0.0, "weight": 0.0})
+
+    for w in (db.query(PlantLabourWorkLog)
+              .filter(PlantLabourWorkLog.log_date >= s_,
+                      PlantLabourWorkLog.log_date <= e).all()):
+        k = w.log_date.isoformat()
+        trend[k]["qty"] += w.qty_nos or 0
+        trend[k]["weight"] += w.weight_kg or 0
+        per_team["Own team"]["qty"] += w.qty_nos or 0
+        per_team["Own team"]["weight"] += w.weight_kg or 0
+    for w in (db.query(PlantContractorWorkLog)
+              .filter(PlantContractorWorkLog.log_date >= s_,
+                      PlantContractorWorkLog.log_date <= e).all()):
+        k = w.log_date.isoformat()
+        trend[k]["qty"] += w.qty_nos or 0
+        trend[k]["weight"] += w.weight_kg or 0
+        name = cmap.get(w.contractor_id, "Unassigned")
+        per_team[name]["qty"] += w.qty_nos or 0
+        per_team[name]["weight"] += w.weight_kg or 0
+
+    # fill every calendar day in range so the trend line has no gaps
+    from datetime import timedelta
+    days = []
+    cur = s_
+    while cur <= e:
+        k = cur.isoformat()
+        t = trend.get(k, {"present": 0, "half": 0, "absent": 0,
+                          "contractor": 0, "qty": 0.0, "weight": 0.0})
+        days.append({"date": k, **t})
+        cur += timedelta(days=1)
+
+    teams = sorted(({"team": n, "qty": round(v["qty"], 2),
+                     "weight": round(v["weight"], 2)}
+                    for n, v in per_team.items()),
+                   key=lambda r: r["weight"], reverse=True)
+
+    totals = {
+        "present": sum(d["present"] for d in days),
+        "half": sum(d["half"] for d in days),
+        "absent": sum(d["absent"] for d in days),
+        "contractor_mandays": sum(d["contractor"] for d in days),
+        "qty": round(sum(d["qty"] for d in days), 2),
+        "weight": round(sum(d["weight"] for d in days), 2),
+    }
+    return {"start": s_.isoformat(), "end": e.isoformat(),
+            "days": days, "teams": teams, "totals": totals}
+
+
 # --------------------------------------------------------------- monthly
 
 def _prev_month(today=None):
diff --git a/app/templates/plant.html b/app/templates/plant.html
index d917132..63c7a8b 100644
--- a/app/templates/plant.html
+++ b/app/templates/plant.html
@@ -312,6 +312,67 @@
     </div>
   </div>
 
+  <!-- ============ DASHBOARD ============ -->
+  <div x-show="tab === 'dashboard'">
+    <div class="flex items-center gap-3 flex-wrap mb-5">
+      <div class="lbl">From</div>
+      <input type="date" x-model="dbStart" @change="loadDashboard()" class="rounded-lg border-metfraa-gray-300 text-sm">
+      <div class="lbl">To</div>
+      <input type="date" x-model="dbEnd" @change="loadDashboard()" class="rounded-lg border-metfraa-gray-300 text-sm">
+      <div class="flex gap-1 ml-auto">
+        <template x-for="q in [[7,'7d'],[30,'30d'],[90,'90d']]" :key="q[0]">
+          <button @click="quickRange(q[0])" class="px-3 py-1.5 rounded-lg ring-1 ring-metfraa-gray-300 text-xs font-bold text-metfraa-gray-600" x-text="q[1]"></button>
+        </template>
+      </div>
+    </div>
+
+    <!-- headline totals -->
+    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3 mb-5" x-show="db">
+      <template x-for="k in kpis" :key="k.label">
+        <div class="bg-white rounded-lg ring-1 ring-metfraa-gray-200 p-4">
+          <div class="lbl" x-text="k.label"></div>
+          <div class="display text-2xl font-bold mt-1" :class="k.cls" x-text="k.val"></div>
+        </div>
+      </template>
+    </div>
+
+    <div class="grid grid-cols-1 lg:grid-cols-2 gap-5 mb-5">
+      <div class="bg-white rounded-lg ring-1 ring-metfraa-gray-200 p-5">
+        <div class="lbl mb-3">Attendance trend</div>
+        <canvas id="attChart" height="150"></canvas>
+      </div>
+      <div class="bg-white rounded-lg ring-1 ring-metfraa-gray-200 p-5">
+        <div class="lbl mb-3">Work done per day</div>
+        <canvas id="workChart" height="150"></canvas>
+      </div>
+    </div>
+
+    <!-- per-team totals -->
+    <div class="bg-white rounded-lg ring-1 ring-metfraa-gray-200 p-5">
+      <div class="lbl mb-3">Output by team</div>
+      <table class="w-full text-sm">
+        <thead><tr class="text-left lbl border-b border-metfraa-gray-100">
+          <th class="py-2">Team</th><th class="py-2 text-right">Qty (Nos)</th>
+          <th class="py-2 text-right">Weight (Kg)</th><th class="py-2 w-1/3">Share by weight</th></tr></thead>
+        <tbody class="divide-y divide-metfraa-gray-100">
+          <template x-for="t in (db ? db.teams : [])" :key="t.team">
+            <tr>
+              <td class="py-2 font-semibold" x-text="t.team"></td>
+              <td class="py-2 text-right mono" x-text="t.qty.toLocaleString('en-IN')"></td>
+              <td class="py-2 text-right mono" x-text="t.weight.toLocaleString('en-IN',{maximumFractionDigits:2})"></td>
+              <td class="py-2"><div class="h-2 rounded-full bg-metfraa-gray-100 overflow-hidden">
+                <div class="h-full bg-metfraa-blue rounded-full" :style="`width:${maxWeight ? (t.weight/maxWeight*100) : 0}%`"></div>
+              </div></td>
+            </tr>
+          </template>
+          <template x-if="db && !db.teams.length">
+            <tr><td colspan="4" class="py-6 text-center text-metfraa-gray-400 text-sm">No work logged in this range.</td></tr>
+          </template>
+        </tbody>
+      </table>
+    </div>
+  </div>
+
   <!-- ============ BROWSE ============ -->
   <div x-show="tab === 'browse'">
     <!-- range roll-up -->
@@ -559,13 +620,16 @@ function plantApp() {
     labour: [], contractors: [], saved: false, saving: false, submitting: false,
     labourOtRate: 50, settingsSaved: false,
     labourWork: [], contractorWork: [],
+    dbStart: new Date(Date.now()-29*864e5).toISOString().slice(0,10),
+    dbEnd: new Date().toISOString().slice(0,10),
+    db: null, attChart: null, workChart: null,
     brStart: new Date(Date.now()-13*864e5).toISOString().slice(0,10),
     brEnd: new Date().toISOString().slice(0,10),
     rangeDays: [], brDay: null,
     labourAll: [], contractorsAll: [], showInactiveLabour: false, showInactiveContr: false,
     drawer: null, form: {},
 
-    tabs: [{ id: 'day', label: 'Attendance' }, { id: 'labour', label: 'Labour Master' },
+    tabs: [{ id: 'day', label: 'Attendance' }, { id: 'dashboard', label: 'Dashboard' }, { id: 'labour', label: 'Labour Master' },
            { id: 'contractors', label: 'Contractor Master' }, { id: 'browse', label: 'Browse' }, { id: 'monthly', label: 'Monthly Report' }, { id: 'settings', label: 'Settings' }],
 
     get presentCount() { return this.labour.filter(l => l.status === 'P').length; },
@@ -605,6 +669,7 @@ function plantApp() {
       if (this.tab === 'contractors') this.loadContractors();
       if (this.tab === 'settings') this.loadSettings();
       if (this.tab === 'browse') { this.loadRange(); }
+      if (this.tab === 'dashboard') { this.loadDashboard(); }
       if (this.tab === 'monthly') { this.loadMonthly(); }
     },
 
@@ -692,6 +757,59 @@ function plantApp() {
       this.form = c ? { ...c } : { name: '', per_day_rate: 0, ot_rate_per_half_hour: 50, active: true };
       this.drawer = 'contractor';
     },
+    get kpis() {
+      if (!this.db) return [];
+      const t = this.db.totals;
+      return [
+        { label: 'Present (man-days)', val: t.present, cls: 'text-green-700' },
+        { label: 'Half days', val: t.half, cls: 'text-amber-600' },
+        { label: 'Absent', val: t.absent, cls: 'text-red-600' },
+        { label: 'Contractor man-days', val: t.contractor_mandays, cls: 'text-metfraa-ink' },
+        { label: 'Total Qty', val: (t.qty||0).toLocaleString('en-IN'), cls: 'text-metfraa-blue' },
+        { label: 'Total Weight (Kg)', val: (t.weight||0).toLocaleString('en-IN',{maximumFractionDigits:0}), cls: 'text-metfraa-blue' },
+      ];
+    },
+    get maxWeight() { return this.db && this.db.teams.length ? Math.max(...this.db.teams.map(t=>t.weight)) : 0; },
+    quickRange(n) {
+      this.dbEnd = new Date().toISOString().slice(0,10);
+      this.dbStart = new Date(Date.now()-(n-1)*864e5).toISOString().slice(0,10);
+      this.loadDashboard();
+    },
+    async loadDashboard() {
+      const r = await fetch(`/plant/api/dashboard?start=${this.dbStart}&end=${this.dbEnd}`);
+      if (!r.ok) return;
+      this.db = await r.json();
+      this.$nextTick(() => this.drawCharts());
+    },
+    drawCharts() {
+      if (typeof Chart === 'undefined') { setTimeout(() => this.drawCharts(), 200); return; }
+      const labels = this.db.days.map(d => d.date.slice(5));
+      if (this.attChart) this.attChart.destroy();
+      this.attChart = new Chart(document.getElementById('attChart'), {
+        type: 'line',
+        data: { labels, datasets: [
+          { label: 'Present', data: this.db.days.map(d=>d.present), borderColor:'#059669', backgroundColor:'rgba(5,150,105,.1)', tension:.3, fill:true },
+          { label: 'Half', data: this.db.days.map(d=>d.half), borderColor:'#d97706', tension:.3 },
+          { label: 'Absent', data: this.db.days.map(d=>d.absent), borderColor:'#dc2626', tension:.3 },
+          { label: 'Contractor', data: this.db.days.map(d=>d.contractor), borderColor:'#1F7CCB', borderDash:[4,3], tension:.3 },
+        ]},
+        options: { responsive:true, maintainAspectRatio:false, interaction:{mode:'index',intersect:false},
+                   plugins:{legend:{position:'bottom',labels:{boxWidth:12,font:{size:11}}}},
+                   scales:{y:{beginAtZero:true,ticks:{precision:0}}} }
+      });
+      if (this.workChart) this.workChart.destroy();
+      this.workChart = new Chart(document.getElementById('workChart'), {
+        type: 'bar',
+        data: { labels, datasets: [
+          { label:'Weight (Kg)', data:this.db.days.map(d=>d.weight), backgroundColor:'#1F7CCB', yAxisID:'y' },
+          { label:'Qty (Nos)', type:'line', data:this.db.days.map(d=>d.qty), borderColor:'#0d1421', tension:.3, yAxisID:'y1' },
+        ]},
+        options: { responsive:true, maintainAspectRatio:false,
+                   plugins:{legend:{position:'bottom',labels:{boxWidth:12,font:{size:11}}}},
+                   scales:{ y:{beginAtZero:true,position:'left',title:{display:true,text:'Kg'}},
+                            y1:{beginAtZero:true,position:'right',grid:{drawOnChartArea:false},title:{display:true,text:'Nos'}} } }
+      });
+    },
     fmtDate(iso) { return new Date(iso+'T00:00').toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'}); },
     async loadRange() {
       const r = await fetch(`/plant/api/browse/range?start=${this.brStart}&end=${this.brEnd}`);
